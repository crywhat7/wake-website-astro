---
interface Props {
  class?: string;
  as?: 'div' | 'section' | 'header' | 'footer' | 'nav';
}

const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-[1200px] px-4 md:px-8', className]}>
  <slot />
</Tag>
